[package]
name = "agentrank-embed"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "AgentRank text embeddings (BGE-compatible dim; hash mode for CI)"

[dependencies]
blake3 = "1.5"

[features]
default = []
