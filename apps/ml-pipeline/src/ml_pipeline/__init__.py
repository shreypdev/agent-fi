"""Offline ML pipelines for AgentRank (embeddings, LTR, eval). Week 1: package skeleton."""

__version__ = "0.1.0"


def placeholder() -> str:
    """Trivial hook for tests and import checks."""
    return "ml-pipeline"
